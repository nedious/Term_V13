<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TermV13"
        tools:targetApi="31">
        <activity
            android:name=".UI.Term.ActivityTermDetail"
            android:label="Term Detail and Course List"
            android:parentActivityName=".UI.Term.ActivityTermList"
            android:exported="false" />
        <activity
            android:name=".UI.Course.ActivityCourseList"
            android:label="Course List"
            android:parentActivityName=".UI.Term.ActivityTermList"
            android:exported="false" />
        <activity
            android:name=".UI.Term.ActivityTermList"
            android:exported="false"
            android:label="Term List"
            android:parentActivityName=".UI.MainActivity" />
        <activity
            android:name=".UI.MainActivity"
            android:exported="true"
            android:label="Home Page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>